{
	"custom/update": {
		"exec": "~/.local/bin/update data",
		"return-type": "json",
		"interval": 3600,
		"signal": 1,
		"min-length": 2,
		"max-length": 2,
		"on-click": "kitty -e ~/.local/bin/update",
		"on-click-right": "pkill -RTMIN+1 waybar"
	}
}
